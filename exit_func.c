#include "main.h"
/**
 * exit_func - exit functionality
 * @sts: sts
 * Return: void
 */
void exit_func()
{
/*	int es;

	if (sts == NULL)
	{
		exit(EXIT_SUCCESS);
	}
	else
	{
		es = atoi(sts);
		exit(es);
	}*/
	exit(EXIT_SUCCESS);
}
